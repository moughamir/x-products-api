{% extends 'admin/layout/base.html.twig' %}

{% block title %}Edit Tag{% endblock %}
{% block page_title %}Edit Tag{% endblock %}

{% block content %}
<div class="p-6">
    <div class="mb-6">
        <a href="/cosmos/admin/tags" class="btn btn-ghost btn-sm">← Back to Tags</a>
    </div>

    <form method="POST" action="/cosmos/admin/tags/{{ tag.id }}" class="space-y-6">
        <div class="card bg-base-100 shadow-md">
            <div class="card-body">
                <div class="form-control">
                    <label class="label"><span class="label-text">Name *</span></label>
                    <input type="text" name="name" value="{{ tag.name }}" required class="input input-bordered">
                </div>
            </div>
        </div>

        <div class="flex gap-4">
            <button type="submit" class="btn btn-primary">Update Tag</button>
            <a href="/cosmos/admin/tags" class="btn btn-ghost">Cancel</a>
        </div>
    </form>
</div>
{% endblock %}

